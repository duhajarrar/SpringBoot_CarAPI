DROP TABLE IF EXISTS Car;

CREATE TABLE Car (
                     id INT AUTO_INCREMENT  PRIMARY KEY,
                     carBrand VARCHAR(250) NOT NULL,
                     countryOfOrigin VARCHAR(250) NOT NULL

);